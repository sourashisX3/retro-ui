package com.funapp.retroui.core.ui.icons

/**
 * Retro pixel icon set.
 *
 * All icons are generated from the Pixelarticons pack (MIT) via the
 * svg-to-compose Gradle plugin (`icons/pixelarticons/`). Each icon is an
 * extension property on this object, e.g. `RetroIcons.Star`. Icons are flat
 * single-path pixel art on a 24x24 grid — tint them with `RetroTheme` colors.
 *
 * Rule: features must use only [RetroIcons] — never Material or third-party
 * icon sets — to keep the retro look consistent.
 */
object RetroIcons